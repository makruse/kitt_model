// kitt
apply plugin: 'application'

mainClassName = "de.zmt.kitt.sim.display.KittWithUI"

startScripts {
    doLast {
        unixScript.text += "echo this has been echoed"
    }
}

dependencies {
    compile group: 'joda-time', name: 'joda-time', version: '2.+'
    compile project(':shared:zmt.general')
    compile project(':shared:zmt.jscience')
}
